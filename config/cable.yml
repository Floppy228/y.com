development:
  adapter: async          # в разработке — встроенная асинхронная шина

test:
  adapter: test

production:
  adapter: solid_cable    # в проде — каналы через базу данных
  polling_interval: 0.1.seconds
  message_retention: 1.day
